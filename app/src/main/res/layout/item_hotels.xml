<?xml version="1.0" encoding="utf-8"?>
<!--This is for designing each row of recycler view -->
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:cardBackgroundColor="#fff"
    app:cardCornerRadius="3dp"
    app:contentPadding="5dp"
    app:cardUseCompatPadding="true"
    tools:context=".HotelsActivity">
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">


            <ImageView
                android:id="@+id/imagePostDet"
                android:layout_width="match_parent"
                android:layout_height="231dp"
                android:adjustViewBounds="true"
                android:background="@drawable/loading_icon"
                android:scaleType="centerCrop" />
            <!-- This image will show till image didn't load (can use loading icon image also) -->
            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:orientation="horizontal"
                android:layout_marginTop="16dp">
                <TextView
                    android:id="@+id/titlePostDet"
                    android:layout_width="match_parent"
                    android:layout_height="60dp"
                    android:text="Title"
                    android:textSize="22sp"
                    android:textStyle="bold"
                    android:textColor="#000">
                </TextView>
                <ImageButton
                    android:id="@+id/button"
                    android:layout_width="64dp"
                    android:layout_height="60dp"
                    android:layout_alignParentRight="true"
                    android:layout_marginStart="280dp"
                    android:background="#fff"
                    android:scaleType="centerInside"
                    android:src="@drawable/icon_location_1"
                    android:textSize="22dp" />
            </RelativeLayout>

            <TextView
                android:id="@+id/descPostDet"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="This will be desc. of destination"
                android:textSize="20sp"
                android:layout_marginTop="5dp"/>

            <Button
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                style="@style/TextAppearance.AppCompat"
                android:layout_marginTop="50dp"
                android:text="Check on Trivago"
                android:id="@+id/trivago" />




        </LinearLayout>
    </ScrollView>
</androidx.cardview.widget.CardView>